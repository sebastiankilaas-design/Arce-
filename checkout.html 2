<!DOCTYPE html>
<html lang="no">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Arce — Velg plan</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant:ital,wght@0,400;0,500;1,400&family=Outfit:wght@300;400;500;600&family=JetBrains+Mono:wght@300;400&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --cream:#F3F1EB;--cream2:#EAE8E1;--white:#FAFAF7;
  --ink:#181612;--ink2:#3C3A34;--ink3:#6E6C65;--ink4:#A09D96;
  --g:#0C4D3A;--g2:#0F6048;--g3:#128058;--gl:#EBF3EE;--gm:#ACCFC0;
  --gold:#B8892A;--gm2:#DFC07A;--gl2:#F5EDDA;
  --char:#1A1917;--char2:#252320;
  --red:#C0392B;--rl:#FEF0EE;
  --brd:#D8D5CC;--brd2:#C5C2B8;
  --sh:0 1px 3px rgba(24,22,18,.07),0 6px 24px rgba(24,22,18,.05);
  --sh2:0 4px 20px rgba(24,22,18,.1),0 24px 64px rgba(24,22,18,.08);
  --serif:'Cormorant',Georgia,serif;
  --body:'Outfit',sans-serif;
  --mono:'JetBrains Mono',monospace;
}
html{font-size:15px}
body{background:var(--cream);color:var(--ink);font-family:var(--body);font-weight:300;min-height:100vh}

/* NAV */
.nav{display:flex;align-items:center;justify-content:space-between;padding:0 2rem;height:60px;background:rgba(243,241,235,.96);backdrop-filter:blur(20px);border-bottom:1px solid var(--brd);position:sticky;top:0;z-index:100}
.logo{display:flex;align-items:center;gap:9px;text-decoration:none}
.logo-mark{width:28px;height:28px;background:var(--char);border-radius:5px;display:flex;align-items:center;justify-content:center}
.logo-word{font-family:var(--serif);font-size:1.15rem;font-weight:500;color:var(--ink);letter-spacing:.02em}
.nav-back{font-size:.8rem;color:var(--ink3);text-decoration:none;display:flex;align-items:center;gap:.35rem;transition:color .15s}
.nav-back:hover{color:var(--ink)}

/* PAGE */
.page{max-width:1100px;margin:0 auto;padding:3.5rem 2rem 5rem}
.page-head{text-align:center;margin-bottom:3rem}
.page-tag{display:inline-flex;align-items:center;gap:7px;font-family:var(--mono);font-size:.62rem;letter-spacing:.1em;text-transform:uppercase;color:var(--g);margin-bottom:.85rem}
.page-tag::before{content:'';display:block;width:16px;height:1px;background:var(--g)}
.page-h{font-family:var(--serif);font-size:clamp(2rem,4vw,3rem);font-weight:400;font-style:italic;color:var(--ink);margin-bottom:.6rem;letter-spacing:-.02em}
.page-sub{font-size:.9rem;color:var(--ink3);max-width:480px;margin:0 auto}

/* BILLING TOGGLE */
.billing-row{display:flex;align-items:center;justify-content:center;gap:.75rem;margin-bottom:2.5rem;font-size:.84rem;color:var(--ink3)}
.tog{width:42px;height:22px;background:var(--brd2);border-radius:100px;cursor:pointer;position:relative;transition:background .2s}
.tog.on{background:var(--g)}
.tog::after{content:'';position:absolute;top:3px;left:3px;width:16px;height:16px;background:#fff;border-radius:50%;transition:transform .2s}
.tog.on::after{transform:translateX(20px)}
.save-pill{background:var(--gl);color:var(--g);border:1px solid var(--gm);border-radius:100px;font-family:var(--mono);font-size:.6rem;padding:2px 8px}

/* PLAN CARDS */
.plans{display:grid;grid-template-columns:repeat(3,1fr);gap:1.5rem}
.plan{background:var(--white);border:1.5px solid var(--brd);border-radius:14px;padding:0;overflow:hidden;display:flex;flex-direction:column;box-shadow:var(--sh);transition:all .2s;position:relative}
.plan:hover{box-shadow:var(--sh2);border-color:var(--brd2)}
.plan.featured{border-color:var(--gm)}
.plan-ribbon{background:var(--g);color:#fff;font-family:var(--mono);font-size:.58rem;letter-spacing:.08em;text-transform:uppercase;text-align:center;padding:.4rem 1rem}
.plan-body{padding:1.75rem;flex:1;display:flex;flex-direction:column}
.plan-name{font-family:var(--serif);font-size:1.35rem;font-weight:500;color:var(--ink);margin-bottom:.2rem;letter-spacing:-.01em}
.plan-for{font-size:.76rem;color:var(--ink3);margin-bottom:1.25rem;line-height:1.55}
.plan-price{display:flex;align-items:baseline;gap:.3rem;margin-bottom:.2rem}
.plan-kr{font-size:1rem;color:var(--ink2);margin-bottom:.25rem}
.plan-amount{font-size:2.5rem;font-weight:300;color:var(--ink);line-height:1;letter-spacing:-.04em}
.plan-period{font-size:.8rem;color:var(--ink3)}
.plan-note{font-size:.68rem;color:var(--ink4);font-family:var(--mono);margin-bottom:1.5rem;min-height:1.4em}
.plan-trial{background:var(--gl);border:1px solid var(--gm);border-radius:6px;padding:.55rem .9rem;font-size:.76rem;color:var(--g);margin-bottom:1.5rem;text-align:center;font-weight:400}
.plan-divider{border:none;border-top:1px solid var(--brd);margin-bottom:1.25rem}
.plan-feats{list-style:none;display:flex;flex-direction:column;gap:.5rem;flex:1;margin-bottom:1.75rem}
.plan-feats li{font-size:.79rem;color:var(--ink3);display:flex;align-items:flex-start;gap:.55rem;line-height:1.5}
.plan-feats li::before{content:'✓';color:var(--g);font-weight:600;flex-shrink:0;margin-top:.05rem}
.plan-feats li.no{color:var(--ink4)}
.plan-feats li.no::before{content:'—';color:var(--brd2)}
.plan-cta{display:block;width:100%;padding:.9rem;border-radius:8px;font-family:var(--body);font-size:.88rem;font-weight:600;cursor:pointer;border:none;text-align:center;text-decoration:none;transition:all .2s;letter-spacing:.01em}
.cta-dark{background:var(--char);color:#fff}
.cta-dark:hover{background:var(--char2);color:#fff}
.cta-green{background:var(--g);color:#fff;box-shadow:0 3px 16px rgba(12,77,58,.22)}
.cta-green:hover{background:var(--g2);color:#fff;transform:translateY(-1px);box-shadow:0 6px 24px rgba(12,77,58,.28)}

/* TRUST STRIP */
.trust{display:flex;align-items:center;justify-content:center;gap:2.5rem;flex-wrap:wrap;margin-top:2.5rem;padding-top:2rem;border-top:1px solid var(--brd)}
.trust-item{display:flex;align-items:center;gap:.5rem;font-size:.78rem;color:var(--ink4)}
.trust-icon{font-size:.9rem}

/* CHECKOUT MODAL */
.modal-overlay{position:fixed;inset:0;z-index:500;background:rgba(24,22,18,.5);backdrop-filter:blur(6px);display:none;align-items:center;justify-content:center;padding:1rem}
.modal-overlay.open{display:flex}
.modal{background:var(--white);border-radius:16px;width:100%;max-width:540px;box-shadow:0 24px 80px rgba(24,22,18,.2);overflow:hidden;animation:modalIn .25s ease both}
@keyframes modalIn{from{opacity:0;transform:translateY(20px) scale(.98)}to{opacity:1;transform:translateY(0) scale(1)}}
.modal-head{padding:1.5rem 1.75rem;border-bottom:1px solid var(--brd);display:flex;align-items:flex-start;justify-content:space-between;gap:1rem}
.modal-title{font-family:var(--serif);font-size:1.3rem;font-style:italic;color:var(--ink)}
.modal-plan-badge{font-family:var(--mono);font-size:.62rem;background:var(--gl);color:var(--g);border:1px solid var(--gm);padding:.2rem .65rem;border-radius:100px;white-space:nowrap;margin-top:.35rem;display:inline-block}
.modal-close{background:none;border:1px solid var(--brd);border-radius:50%;width:28px;height:28px;cursor:pointer;font-size:.95rem;color:var(--ink3);display:flex;align-items:center;justify-content:center;flex-shrink:0;transition:all .15s}
.modal-close:hover{background:var(--cream2);color:var(--ink)}
.modal-body{padding:1.5rem 1.75rem}
.modal-price-row{display:flex;align-items:center;justify-content:space-between;background:var(--cream);border:1px solid var(--brd);border-radius:8px;padding:.85rem 1.1rem;margin-bottom:1.5rem}
.modal-price-label{font-size:.8rem;color:var(--ink2)}
.modal-price-val{font-family:var(--serif);font-size:1.3rem;font-style:italic;color:var(--ink)}
.modal-price-period{font-size:.72rem;color:var(--ink4)}

/* FORM */
.form-row{display:grid;grid-template-columns:1fr 1fr;gap:.75rem;margin-bottom:.75rem}
.form-row.full{grid-template-columns:1fr}
.field label{display:block;font-size:.72rem;font-weight:500;color:var(--ink2);margin-bottom:.38rem;letter-spacing:.01em}
.inp{width:100%;background:var(--cream);border:1px solid var(--brd2);border-radius:7px;padding:.65rem .9rem;font-family:var(--body);font-size:.84rem;color:var(--ink);outline:none;transition:all .18s}
.inp:focus{border-color:var(--g);background:var(--white);box-shadow:0 0 0 3px rgba(12,77,58,.08)}
.inp::placeholder{color:var(--ink4)}
.sel{width:100%;background:var(--cream);border:1px solid var(--brd2);border-radius:7px;padding:.65rem .9rem;font-family:var(--body);font-size:.84rem;color:var(--ink);outline:none;cursor:pointer;transition:border-color .18s}
.sel:focus{border-color:var(--g)}

/* CARD INPUT AREA */
.card-section{margin-bottom:1.25rem}
.card-label{font-size:.72rem;font-weight:500;color:var(--ink2);margin-bottom:.38rem;letter-spacing:.01em;display:block}
.stripe-card-mock{background:var(--cream);border:1px solid var(--brd2);border-radius:7px;padding:.65rem .9rem;display:flex;align-items:center;justify-content:space-between;min-height:44px;cursor:text}
.stripe-card-real{background:var(--cream);border:1px solid var(--brd2);border-radius:7px;padding:.65rem .9rem;min-height:44px;transition:all .18s}
.stripe-card-real.StripeElement--focus{border-color:var(--g);background:var(--white);box-shadow:0 0 0 3px rgba(12,77,58,.08)}
.card-icons{display:flex;gap:.35rem;align-items:center;opacity:.5}
.card-icon{font-size:.85rem}

/* TRIAL NOTE */
.trial-note{background:var(--gl);border:1px solid var(--gm);border-radius:8px;padding:.75rem 1rem;font-size:.76rem;color:var(--g);line-height:1.6;margin-bottom:1.25rem}
.trial-note strong{font-weight:500}

/* SUBMIT */
.modal-submit{width:100%;padding:.9rem;background:var(--g);color:#fff;border:none;border-radius:8px;font-family:var(--body);font-size:.92rem;font-weight:600;cursor:pointer;transition:all .18s;box-shadow:0 3px 16px rgba(12,77,58,.2);display:flex;align-items:center;justify-content:center;gap:.6rem;letter-spacing:.01em}
.modal-submit:hover{background:var(--g2);transform:translateY(-1px);box-shadow:0 6px 24px rgba(12,77,58,.28)}
.modal-submit:disabled{opacity:.45;cursor:not-allowed;transform:none;box-shadow:none}
.spinner{width:16px;height:16px;border:2px solid rgba(255,255,255,.3);border-top-color:#fff;border-radius:50%;animation:spin .7s linear infinite;display:none}
@keyframes spin{to{transform:rotate(360deg)}}
.modal-micro{text-align:center;font-size:.7rem;color:var(--ink4);margin-top:.75rem;display:flex;align-items:center;justify-content:center;gap:.5rem}

/* ERROR */
.stripe-error{color:var(--red);font-size:.75rem;margin-top:.5rem;display:none;padding:.5rem .75rem;background:var(--rl);border-radius:6px}

/* SUCCESS SCREEN */
.success-screen{display:none;flex-direction:column;align-items:center;text-align:center;padding:2.5rem 1.75rem}
.success-icon{width:60px;height:60px;border-radius:50%;background:var(--gl);border:1.5px solid var(--gm);display:flex;align-items:center;justify-content:center;font-size:1.5rem;margin-bottom:1.25rem;animation:pop .35s ease}
@keyframes pop{0%{transform:scale(.5);opacity:0}70%{transform:scale(1.1)}100%{transform:scale(1);opacity:1}}
.success-title{font-family:var(--serif);font-size:1.6rem;font-style:italic;color:var(--ink);margin-bottom:.5rem}
.success-sub{font-size:.84rem;color:var(--ink3);line-height:1.7;margin-bottom:1.75rem;max-width:380px}
.success-next{display:flex;flex-direction:column;gap:.65rem;width:100%}
.success-btn{display:block;width:100%;padding:.8rem;border-radius:8px;font-family:var(--body);font-size:.88rem;font-weight:500;cursor:pointer;text-decoration:none;text-align:center;transition:background .18s;border:none}
.sb-primary{background:var(--g);color:#fff}.sb-primary:hover{background:var(--g2);color:#fff}
.sb-ghost{background:transparent;color:var(--ink2);border:1px solid var(--brd2)}.sb-ghost:hover{background:var(--cream2)}

/* RESPONSIVE */
@media(max-width:780px){
  .plans{grid-template-columns:1fr;max-width:420px;margin:0 auto}
  .page{padding:2.5rem 1.25rem 4rem}
  .form-row{grid-template-columns:1fr}
}
@media(max-width:480px){
  .trust{flex-direction:column;gap:1rem;align-items:flex-start;padding-left:1rem}
  .modal{border-radius:12px}
}
</style>
</head>
<body>

<nav class="nav">
  <a class="logo" href="index.html">
    <div class="logo-mark">
      <svg width="16" height="16" viewBox="0 0 18 18" fill="none">
        <path d="M9 2L16 6v6l-7 4L2 12V6l7-4z" stroke="#F3F1EB" stroke-width="1.4" fill="none" stroke-linejoin="round"/>
        <path d="M9 2v10M2 6l7 4 7-4" stroke="#F3F1EB" stroke-width="1.1" stroke-linejoin="round"/>
      </svg>
    </div>
    <div class="logo-word">Arce</div>
  </a>
  <a class="nav-back" href="index.html">← Tilbake</a>
</nav>

<div class="page">
  <div class="page-head">
    <div class="page-tag">Velg plan</div>
    <h1 class="page-h">Start i dag. Se resultater i morgen.</h1>
    <p class="page-sub">14 dager gratis på alle planer. Kortet ditt belastes ikke før prøveperioden er over.</p>
  </div>

  <!-- BILLING TOGGLE -->
  <div class="billing-row">
    <span>Månedlig</span>
    <div class="tog" id="billingTog" onclick="toggleBilling()"></div>
    <span>Årlig <span class="save-pill">Spar 20%</span></span>
  </div>

  <!-- PLAN CARDS -->
  <div class="plans">

    <!-- SOLO -->
    <div class="plan">
      <div class="plan-body">
        <div class="plan-name">Solo</div>
        <div class="plan-for">For deg som jobber alene og vil slutte å miste leads til konkurrenten.</div>
        <div class="plan-price">
          <span class="plan-kr">kr</span>
          <span class="plan-amount" id="a1">3 490</span>
          <span class="plan-period">/mnd</span>
        </div>
        <div class="plan-note" id="n1">Fakturert månedlig</div>
        <div class="plan-trial">✓ 14 dager gratis — ingen belastning nå</div>
        <hr class="plan-divider">
        <ul class="plan-feats">
          <li>50 bookede avtaler / mnd</li>
          <li>1 bruker · 1 kalender</li>
          <li>E-postkommunikasjon</li>
          <li>Lead-kvalifisering</li>
          <li>Automatiske påminnelser</li>
          <li>Responstid &lt;15 min</li>
          <li>Chat-support</li>
          <li class="no">SMS & WhatsApp</li>
          <li class="no">CRM-integrasjon</li>
          <li class="no">Lead Intelligence</li>
        </ul>
        <button class="plan-cta cta-dark" onclick="openCheckout('solo')">Start gratis prøveperiode</button>
      </div>
    </div>

    <!-- VEKST -->
    <div class="plan featured">
      <div class="plan-ribbon">Mest populær</div>
      <div class="plan-body">
        <div class="plan-name">Vekst</div>
        <div class="plan-for">For team som vil skalere salget uten å ansette flere bookingfolk.</div>
        <div class="plan-price">
          <span class="plan-kr">kr</span>
          <span class="plan-amount" id="a2">5 490</span>
          <span class="plan-period">/mnd</span>
        </div>
        <div class="plan-note" id="n2">Fakturert månedlig</div>
        <div class="plan-trial">✓ 14 dager gratis — ingen belastning nå</div>
        <hr class="plan-divider">
        <ul class="plan-feats">
          <li><strong>Ubegrensede</strong> bookede avtaler</li>
          <li>5 brukere · 10 kalendere</li>
          <li>E-post + SMS + WhatsApp</li>
          <li>Avansert kvalifisering</li>
          <li>Responstid &lt;2 min</li>
          <li>CRM-integrasjon (valgfritt)</li>
          <li>Lead Intelligence inkludert</li>
          <li>Analytikk-dashboard</li>
          <li>A/B-testing av meldinger</li>
          <li>Prioritert support</li>
        </ul>
        <button class="plan-cta cta-green" onclick="openCheckout('vekst')">Start gratis prøveperiode</button>
      </div>
    </div>

    <!-- AGENCY -->
    <div class="plan">
      <div class="plan-body">
        <div class="plan-name">Agency</div>
        <div class="plan-for">For byråer som vil selge Arce til egne kunder under eget navn.</div>
        <div class="plan-price">
          <span class="plan-kr">kr</span>
          <span class="plan-amount" id="a3">8 990</span>
          <span class="plan-period">/mnd</span>
        </div>
        <div class="plan-note" id="n3">Fakturert månedlig</div>
        <div class="plan-trial">✓ 14 dager gratis — ingen belastning nå</div>
        <hr class="plan-divider">
        <ul class="plan-feats">
          <li><strong>Alt i Vekst</strong>, pluss:</li>
          <li>Ubegrenset brukere</li>
          <li>White-label — ditt navn</li>
          <li>Full API-tilgang</li>
          <li>AI trent på din bransje</li>
          <li>Dedikert account manager</li>
          <li>SLA 99,9% oppetid</li>
          <li>Kvartalsvise strategimøter</li>
          <li>Tidligst nye funksjoner</li>
          <li>Custom onboarding</li>
        </ul>
        <button class="plan-cta cta-dark" onclick="openCheckout('agency')">Start gratis prøveperiode</button>
      </div>
    </div>

  </div><!-- /plans -->

  <!-- TRUST STRIP -->
  <div class="trust">
    <div class="trust-item"><span class="trust-icon">🔒</span> Sikker betaling via Stripe</div>
    <div class="trust-item"><span class="trust-icon">✓</span> Ingen binding — avslutt når som helst</div>
    <div class="trust-item"><span class="trust-icon">✓</span> 14 dager gratis — kortet belastes ikke nå</div>
    <div class="trust-item"><span class="trust-icon">🇳🇴</span> Faktura i NOK · MVA inkludert</div>
  </div>
</div><!-- /page -->

<!-- CHECKOUT MODAL -->
<div class="modal-overlay" id="modalOverlay" onclick="handleOverlayClick(event)">
  <div class="modal" id="modal">

    <!-- FORM VIEW -->
    <div id="formView">
      <div class="modal-head">
        <div>
          <div class="modal-title" id="modalTitle">Start prøveperiode</div>
          <div class="modal-plan-badge" id="modalBadge">Vekst · 14 dager gratis</div>
        </div>
        <button class="modal-close" onclick="closeCheckout()">✕</button>
      </div>
      <div class="modal-body">

        <!-- Price summary -->
        <div class="modal-price-row">
          <div>
            <div class="modal-price-label" id="modalPriceLabel">Vekst-plan</div>
            <div class="modal-price-period" id="modalPricePeriod">Månedlig · starter etter 14 dager</div>
          </div>
          <div class="modal-price-val" id="modalPriceVal">kr 5 490/mnd</div>
        </div>

        <!-- Account info -->
        <div class="form-row">
          <div class="field"><label>Fornavn *</label><input class="inp" id="cfirst" placeholder="Kari" autocomplete="given-name"/></div>
          <div class="field"><label>Etternavn *</label><input class="inp" id="clast" placeholder="Nilsen" autocomplete="family-name"/></div>
        </div>
        <div class="form-row full" style="margin-bottom:.75rem">
          <div class="field"><label>E-post *</label><input class="inp" id="cemail" type="email" placeholder="kari@bedrift.no" autocomplete="email"/></div>
        </div>
        <div class="form-row full" style="margin-bottom:.75rem">
          <div class="field"><label>Bedriftsnavn</label><input class="inp" id="ccompany" placeholder="Bedrift AS" autocomplete="organization"/></div>
        </div>

        <!-- Payment -->
        <div class="card-section">
          <label class="card-label">Betalingskort *</label>
          <!-- Stripe Elements mounts here -->
          <div id="card-element" class="stripe-card-real"></div>
          <div class="stripe-error" id="card-error"></div>
        </div>

        <!-- Trial note -->
        <div class="trial-note">
          <strong>Ingen belastning i dag.</strong> Prøveperioden er gratis i 14 dager. Etter det belastes kortet automatisk. Du kan avslutte når som helst fra dashboardet ditt.
        </div>

        <button class="modal-submit" id="submitBtn" onclick="handleSubmit()">
          <div class="spinner" id="spinner"></div>
          <span id="submitLabel">Start 14 dager gratis</span>
        </button>
        <div class="modal-micro">
          <span>🔒</span> Sikret med Stripe · Kryptert betaling · GDPR-compliant
        </div>
      </div>
    </div>

    <!-- SUCCESS VIEW -->
    <div class="success-screen" id="successView">
      <div class="success-icon">✓</div>
      <div class="success-title" id="successTitle">Konto opprettet!</div>
      <p class="success-sub" id="successSub">Vi sender deg en velkomst-e-post med tilgang til dashboardet ditt. Prøveperioden din er aktiv — ingen belastning de neste 14 dagene.</p>
      <div class="success-next">
        <a class="success-btn sb-primary" href="onboarding.html">Sett opp Arce nå →</a>
        <a class="success-btn sb-ghost" href="dashboard.html">Gå til dashboardet</a>
      </div>
    </div>

  </div><!-- /modal -->
</div><!-- /overlay -->

<!-- Stripe.js -->
<script src="https://js.stripe.com/v3/"></script>
<script>
/*
  ═══════════════════════════════════════════════
  STRIPE OPPSETT
  ═══════════════════════════════════════════════
  1. Gå til dashboard.stripe.com og opprett en konto
  2. Kopier din "Publishable key" fra Developers → API keys
  3. Lim den inn under:
*/
var STRIPE_PK = 'pk_test_SETT_INN_DIN_STRIPE_PUBLISHABLE_KEY_HER';

/*
  PRICE IDs (fra Stripe Dashboard → Products)
  Opprett 6 produkter i Stripe (3 planer × månedlig/årlig):
*/
var PRICE_IDS = {
  solo_monthly:   'price_SOLO_MONTHLY_ID',
  solo_yearly:    'price_SOLO_YEARLY_ID',
  vekst_monthly:  'price_VEKST_MONTHLY_ID',
  vekst_yearly:   'price_VEKST_YEARLY_ID',
  agency_monthly: 'price_AGENCY_MONTHLY_ID',
  agency_yearly:  'price_AGENCY_YEARLY_ID'
};

/* ─── STATE ─── */
var isYearly = false;
var currentPlan = 'vekst';
var stripe = null;
var cardElement = null;

var PLANS = {
  solo:   { name:'Solo',   monthly:3490, yearly:2792, monthlyId:'solo_monthly',   yearlyId:'solo_yearly'   },
  vekst:  { name:'Vekst',  monthly:5490, yearly:4392, monthlyId:'vekst_monthly',  yearlyId:'vekst_yearly'  },
  agency: { name:'Agency', monthly:8990, yearly:7192, monthlyId:'agency_monthly', yearlyId:'agency_yearly' }
};

/* ─── BILLING TOGGLE ─── */
function toggleBilling(){
  isYearly = !isYearly;
  document.getElementById('billingTog').classList.toggle('on', isYearly);
  updatePriceDisplay();
}

function fmt(n){ return n.toLocaleString('nb-NO'); }

function updatePriceDisplay(){
  var data = [
    {aId:'a1', nId:'n1', plan:'solo'},
    {aId:'a2', nId:'n2', plan:'vekst'},
    {aId:'a3', nId:'n3', plan:'agency'}
  ];
  data.forEach(function(d){
    var p = PLANS[d.plan];
    var amount = isYearly ? p.yearly : p.monthly;
    document.getElementById(d.aId).textContent = fmt(amount);
    document.getElementById(d.nId).textContent = isYearly
      ? 'Fakturert årlig · spar ' + fmt((p.monthly - p.yearly)*12) + ' kr/år'
      : 'Fakturert månedlig';
  });
}

/* ─── MODAL ─── */
function openCheckout(plan){
  currentPlan = plan;
  var p = PLANS[plan];
  var amount = isYearly ? p.yearly : p.monthly;
  var period = isYearly ? 'Årlig' : 'Månedlig';

  document.getElementById('modalTitle').textContent = 'Start ' + p.name + '-plan';
  document.getElementById('modalBadge').textContent = p.name + ' · 14 dager gratis';
  document.getElementById('modalPriceLabel').textContent = p.name + '-plan';
  document.getElementById('modalPricePeriod').textContent = period + ' · starter etter 14 dager';
  document.getElementById('modalPriceVal').textContent = 'kr ' + fmt(amount) + '/mnd';
  document.getElementById('submitLabel').textContent = 'Start 14 dager gratis';

  document.getElementById('formView').style.display = 'block';
  document.getElementById('successView').style.display = 'none';
  document.getElementById('modalOverlay').classList.add('open');
  document.body.style.overflow = 'hidden';

  // Mount Stripe card element
  initStripe();
}

function closeCheckout(){
  document.getElementById('modalOverlay').classList.remove('open');
  document.body.style.overflow = '';
  clearErrors();
}

function handleOverlayClick(e){
  if(e.target === document.getElementById('modalOverlay')) closeCheckout();
}

function clearErrors(){
  var el = document.getElementById('card-error');
  el.style.display = 'none';
  el.textContent = '';
}

function showError(msg){
  var el = document.getElementById('card-error');
  el.textContent = msg;
  el.style.display = 'block';
}

/* ─── STRIPE INIT ─── */
function initStripe(){
  if(cardElement) return; // already mounted
  try {
    stripe = Stripe(STRIPE_PK);
    var elements = stripe.elements({
      fonts:[{cssSrc:'https://fonts.googleapis.com/css2?family=Outfit:wght@300;400&display=swap'}]
    });
    cardElement = elements.create('card', {
      style:{
        base:{
          fontFamily:"'Outfit', sans-serif",
          fontSize:'14px',
          color:'#181612',
          fontWeight:'300',
          '::placeholder':{color:'#A09D96'}
        },
        invalid:{color:'#C0392B'}
      },
      hidePostalCode: true
    });
    cardElement.mount('#card-element');
    cardElement.on('change', function(e){
      if(e.error) showError(e.error.message);
      else clearErrors();
    });
  } catch(e) {
    // Stripe not configured — show demo mode
    document.getElementById('card-element').innerHTML =
      '<div style="font-size:.78rem;color:var(--ink4);font-family:var(--mono);padding:.2rem 0">⚠ Stripe ikke konfigurert — demo-modus</div>';
  }
}

/* ─── SUBMIT ─── */
async function handleSubmit(){
  clearErrors();
  var first = document.getElementById('cfirst').value.trim();
  var last  = document.getElementById('clast').value.trim();
  var email = document.getElementById('cemail').value.trim();

  if(!first){ showError('Skriv inn fornavn.'); return; }
  if(!email || !email.includes('@')){ showError('Skriv inn en gyldig e-postadresse.'); return; }

  setLoading(true);

  // If Stripe not configured → demo mode
  if(!stripe || !cardElement){
    await demoDelay(1800);
    showSuccess(first, email);
    setLoading(false);
    return;
  }

  try {
    // 1. Create PaymentMethod
    var company = document.getElementById('ccompany').value.trim();
    var result = await stripe.createPaymentMethod({
      type: 'card',
      card: cardElement,
      billing_details:{
        name: first + ' ' + last,
        email: email,
        company: company || undefined
      }
    });

    if(result.error){
      showError(result.error.message);
      setLoading(false);
      return;
    }

    /*
      2. Send to your backend:
      POST /api/create-subscription with:
      {
        paymentMethodId: result.paymentMethod.id,
        email: email,
        name: first + ' ' + last,
        company: company,
        priceId: PRICE_IDS[PLANS[currentPlan][isYearly ? 'yearlyId' : 'monthlyId']],
        trialDays: 14
      }

      Your backend (Node/Vercel/Supabase Edge Function) should:
      1. Create Stripe Customer
      2. Attach PaymentMethod to Customer
      3. Create Subscription with trial_period_days: 14
      4. Return { clientSecret, subscriptionId }

      For now: demo success
    */

    await demoDelay(1500);
    showSuccess(first, email);

  } catch(e){
    showError('Noe gikk galt. Prøv igjen eller kontakt oss.');
  }

  setLoading(false);
}

function demoDelay(ms){ return new Promise(function(r){ setTimeout(r, ms); }); }

function setLoading(on){
  var btn = document.getElementById('submitBtn');
  var sp  = document.getElementById('spinner');
  var lbl = document.getElementById('submitLabel');
  btn.disabled = on;
  sp.style.display  = on ? 'block' : 'none';
  lbl.textContent   = on ? 'Oppretter konto…' : 'Start 14 dager gratis';
}

function showSuccess(first, email){
  document.getElementById('formView').style.display = 'none';
  var sv = document.getElementById('successView');
  sv.style.display = 'flex';
  document.getElementById('successTitle').textContent = 'Velkommen, ' + first + '!';
  document.getElementById('successSub').textContent =
    'Vi sender tilgang til ' + email + ' innen få minutter. '
    + 'Prøveperioden din er aktiv — ingen belastning de neste 14 dagene.';
}

/* Keyboard close */
document.addEventListener('keydown', function(e){
  if(e.key === 'Escape') closeCheckout();
});
</script>
</body>
</html>
